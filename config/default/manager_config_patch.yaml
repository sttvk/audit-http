apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: default
spec:
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    spec:
      containers:
        - name: manager
